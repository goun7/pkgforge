# PkgForge — Core dependencies
# All core modules work without PyQt6. Only GUI mode requires it.

# ── Core (required) ────────────────────────────────────────────
# No external dependencies required for CLI operation.
# All features use Python stdlib + system tools (pacman, makepkg, etc.)

# ── GUI (optional) ─────────────────────────────────────────────
# Install for: pkgforge gui
PyQt6>=6.5.0; python_version>="3.10"

# ── Optional: D-Bus service bridge (C1) ───────────────────────
# Pure-Python; install to enable `pkgforge serve --dbus` / dbus.start
# jeepney>=0.8

# ── Development ────────────────────────────────────────────────
# Install with: pip install -r requirements-dev.txt
# pytest>=7.0
# pytest-cov>=4.0
# pytest-timeout>=2.0
# mypy>=1.0
# bandit>=1.7
